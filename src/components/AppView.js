import React from 'react'

function AppView({appId, formId}) {
    return (
        <div>
            <div>버튼영역</div>
            <div>본문영역 -  * 파일수정 수시로?(본문 생성 안하고, 매핑만 진행하는 방향으로 - 저장액션에서만 파일 생성 )</div>
        </div>
    )
}

export default AppView
